import { StyleSheet } from "react-native";

const size = 60;

const styles = StyleSheet.create({
    containter: {
        backgroundColor: '#fff',
        borderRadius: size/2,
    },

    emoji: {
        width: size,
        height: size,
    }
});

export default styles;