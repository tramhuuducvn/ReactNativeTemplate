import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fff',
        paddingHorizontal: 2,
        borderRadius: 100,
        position: 'absolute',
    }
});


export default styles;